{
  "name": "trivy-reporting-extenstion",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd trivy-reporter-task && npx rimraf *.js && tsc",
    "build:ext": "tfx extension create --manifest-globs vss-extension.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "got": "^12.5.2",
    "vss-web-extension-sdk": "^5.141.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
